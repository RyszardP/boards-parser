CREATE TABLE `board_games`.`nastolki_by` (
                                             `id` INT NOT NULL AUTO_INCREMENT,
                                             `title` VARCHAR(255) NULL,
                                             `price` VARCHAR(10) NULL,
                                             `age` VARCHAR(45) NULL,
                                             `gamers_countity` VARCHAR(45) NULL,
                                             `link` VARCHAR(250) NULL,
                                             PRIMARY KEY (`id`));